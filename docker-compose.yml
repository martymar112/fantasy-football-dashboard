version: "3.9"

services:
  fantasy-dashboard:
    build: .
    container_name: fantasy-dashboard
    env_file:
      - .env
    ports:
     - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
    command: python main.py
    restart: unless-stopped
